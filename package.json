{
  "name": "tps-finances",
  "version": "0.5.1",
  "description": "Contract-native account, transaction, and investment management for TPS.",
  "main": "main.js",
  "scripts": {
    "build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production",
    "test": "node --test scripts/test-finance-contract.mjs && npm run build"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^16.11.6",
    "builtin-modules": "3.3.0",
    "esbuild": "^0.28.1",
    "obsidian": "latest",
    "typescript": "4.7.4"
  }
}
