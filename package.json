{
  "name": "tps-finances",
  "version": "1.3.2",
  "description": "Accounts, transactions, and investments as atomic notes for TPS.",
  "main": "main.js",
  "scripts": {
    "build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production",
    "test": "node --test scripts/test-finance-contract.mjs scripts/test-atomic-finance.mjs scripts/test-manual-finance.mjs scripts/test-mobile-loading.mjs && npm run build"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^16.11.6",
    "builtin-modules": "3.3.0",
    "esbuild": "^0.28.1",
    "obsidian": "latest",
    "typescript": "4.7.4"
  }
}
