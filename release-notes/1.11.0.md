Finances supports GCM tag mappings through the existing property codec. Generated Base predicates use file.hasTag; legacy property mappings remain compatible.

This is a backward-compatible minor release: tag mappings are opt-in, existing property mappings keep working, and upgrades do not migrate notes or settings automatically. Complete tag paths remain configurable. Conflicting record-type tags are rejected.

Validation: 258 tests passed, including account creation, transaction import/update/readback and generated Base filters. Targeted reload verified the installed test-vault version. TypeScript, full declared tests, the mandatory separate final build and test-vault deployment passed. The affected plugins were reloaded by manifest ID. No provider calls or production writes were part of QA.

Minimum Obsidian: 1.12.0. Health and Finances require GCM 3.6.0 when using tag mappings. Existing notes, templates, Bases and settings references must be migrated together after compatible runtimes are installed. GCM’s tag-value editor renames exact frontmatter values and matching GCM references; user-authored Base formulas and external-plugin references still require review. Physical iPhone acceptance remains outstanding.

Tested in Obsidian Plugin Test Vault and ready for the user's BRAT pull. This release is not claimed to be installed in production.

SHA-256:

- `main.js`: `25714a936ed6141d176ba24538128bd54b15a87617b57ece8939d05cf554782a`
- `manifest.json`: `f2feb36f8f7eab4b5ca384e0980119cf83387a6ca2da6e472286aaf8f091f918`
- `styles.css`: `0f8262115af5ff9ea63137dfb388dde3cca3b97dbfc733d298de494660034a7e`
